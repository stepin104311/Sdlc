#include "../inc/sdlc.h"
int squareroot(int a)
{
		return sqrt(a);
}
float reciprocal(int a)
{
		    return 1.0/a;
}
int power(int a,int b)
{
    return pow(a,b);
}
float exponential(int a)
{

    return exp(a);
}
